<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="CAppTheme" parent="android:Theme.Holo.Light.NoActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="DAppTheme" parent="@style/Theme.AppCompat">
        <!-- Customize your theme here. -->
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@color/cwhite</item>
        <item name="android:textColor">@color/baclk</item>
    </style>

</resources>
